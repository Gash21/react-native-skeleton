
const initialState = {
    version: '1.0.1',
    majorVersion: 1,
    minorVersion: 0,
    patchVersion: 1
}

const basic = (state = initialState, action) => {
    
    switch(action.type){
        default :
            return state;
    }
}


export default basic;